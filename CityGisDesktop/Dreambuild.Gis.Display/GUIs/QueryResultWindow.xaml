<Window x:Class="Dreambuild.Gis.Display.QueryResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="查询结果" Height="600" Width="800" WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    
    <Window.Resources>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Maroon" />
    </Window.Resources>
    
    <Grid x:Name="LayoutRoot" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="CancelButton" Content="取消" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="确定" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
        <DataGrid AutoGenerateColumns="False" Margin="0" Name="dataGrid1" HorizontalGridLinesBrush="Silver" VerticalGridLinesBrush="Silver" IsReadOnly="True" AlternatingRowBackground="WhiteSmoke" />
    </Grid>
    
</Window>
