@using TongJi.Web.CMS
@{
    int id = Convert.ToInt32(Request.Params["id"]);
    var record = VoteManager.GetDbRecord(id);
    record.can_vote = !record.can_vote;
    VoteManager.SaveChanges();
    Response.Redirect(string.Format("List"));
}
